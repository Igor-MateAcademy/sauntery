projects:
  saunter:
    schemaPath: amplify/backend/api/saunter/build/schema.graphql
    includes:
      - genereted
    excludes:
      - ./amplify/**
    extensions:
      amplify:
        codeGenTarget: typescript
        generatedFileName: src/API.ts
        docsFilePath: .
extensions:
  amplify:
    version: 3
